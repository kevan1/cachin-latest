# Cachin Landing

Next.js landing page generated from the Claude mockup at `/Users/kevan/Downloads/cachin-landing (1).html`.

## Run

```bash
npm run dev
```

If the shell picks the broken Homebrew Node install, use the working NVM Node path:

```bash
PATH=/Users/kevan/.nvm/versions/node/v24.8.0/bin:$PATH npm run dev
```

## Scripts

- `npm run dev` - start local development
- `npm run build` - production build
- `npm run lint` - run ESLint

## Notes

The copy keeps the mockup's visual direction while tightening public claims around the current product guardrails: Argentina MVP first, broader LATAM coverage as roadmap, and card/contactless as a complementary surface.
